pragma solidity 0.8.0;

contract variables{
    uint256 public u;
    uint public u
    uint16 public u1;//just use uint256 instead of uint16 and uint32 pls uses less gas in the the end
    uint32 public u3;
    string public m;
    bytes public b;
    bytes32 public b32;//same goes for this
}